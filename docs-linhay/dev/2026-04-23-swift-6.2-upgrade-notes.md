# Swift 6.2 升级记录

日期：2026-04-23
分支：`codex/upgrade-swift6`

## 本次完成

- `Package.swift` 升级到 `swift-tools-version: 6.2`
- package 开启 `swiftLanguageModes: [.v6]`
- UIKit / SectionUI 桥接层补充一批 `@MainActor` 与并发安全修正，使 Example Demo 可编译运行
- `SectionKit2.podspec` 与 `SectionUI.podspec` 的 `swift_version` 同步到 `6.2`
- 修正 `SKPublished` 相关测试以适配 Swift 6.2 并发检查与 `any Publisher` 行为

## 验证结果

- `Example` scheme 已在 iOS Simulator 上成功构建、安装、启动
- Demo 首页可见，且可进入 `Lesson 1: Standard Cell` 示例页
- `SectionKit-Package` 测试通过：18 passed, 0 failed
- `Example.xcodeproj` 已切换到 Swift 6：
  - `SWIFT_VERSION = 6.0`
  - `EFFECTIVE_SWIFT_VERSION = 6`

## 已知剩余项

- 仍有一批 Swift 6 并发 warning，主要集中在旧 UIKit API 的非隔离调用
- `SKPublishedTests.swift` 仍有一个低风险 warning：
  - `weakTarget` 可改成 `let`
- 若后续继续推进 Swift 6 完整收敛，建议优先处理：
  - `Sources/SectionKit/Common/SKPublished.swift`
  - `Sources/SectionKit/CollectionBase/SKSafeSizeProviderProtocol.swift`
  - `Sources/SectionUI/Sections/SKCAnyViewCell.swift`
  - `Sources/SectionUI/Sections/SKCSectionViewCell.swift`
  - `Sources/SectionUI/CollectionView/SectionCollectionView/FlowLayout/SKCollectionFlowLayout.swift`

## 追加收敛：Actor Isolation Warnings

- 已继续清理一批 Swift 6 actor isolation warning，重点文件如下：
  - `Sources/SectionKit/CollectionBase/SKSafeSizeProviderProtocol.swift`
  - `Sources/SectionKit/CollectionSingleTypeSection/Entities/SKCSingleTypeSection+refresh.swift`
  - `Sources/SectionUI/CollectionView/SectionCollectionView/FlowLayout/SKCLayoutPlugins.swift`
  - `Sources/SectionUI/CollectionView/SectionCollectionView/FlowLayout/SKCollectionFlowLayout.swift`
  - `Sources/SectionUI/CollectionView/SectionCollectionView/SKCLayoutDecorationPlugin/SKCLayoutDecorationPlugin.swift`
  - `Sources/SectionUI/SKBindingKey.swift`
  - `Sources/SectionUI/Sections/Plugin+Pin.swift`
  - `Sources/SectionUI/Sections/SKCAnyViewCell.swift`
  - `Sources/SectionUI/Sections/SKCSectionViewCell.swift`
- 处理方式以“显式主 actor 化 UIKit/布局回调、避免把非 Sendable 值送入隔离闭包”为主，没有改变 Demo 行为。
- 当前 `SectionKit-Package` 测试输出中已不再出现 actor isolation warning。
- 当前 `Example` 构建仍有 warning，但已不再是 actor isolation 类型，剩余主要是：
  - beta API 的 deprecation warning
  - 示例代码中的未使用变量
  - 旧 UIKit API 的普通弃用提示

## 追加收口：Example 工程切换到 Swift 6

- `Example.xcodeproj/project.pbxproj` 中 Debug / Release 的 `SWIFT_VERSION` 已从 `5.0` 更新为 `6.0`
- 重新验证 `xcodebuild -project Example.xcodeproj -scheme Example -showBuildSettings`，输出确认：
  - `SWIFT_VERSION = 6.0`
  - `EFFECTIVE_SWIFT_VERSION = 6`
- 切换后 `Example` 仍可成功构建并运行，没有新增 Swift 6 编译阻塞
