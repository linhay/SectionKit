# 2026-04-23

## Swift 6.2 升级试探

- 在 `codex/upgrade-swift6` 分支将 `Package.swift` 升级到 `swift-tools-version: 6.2`，并启用 `swiftLanguageModes: [.v6]`。
- 已完成第一批 Swift 6 适配，重点集中在 `Sendable` 收缩、`@MainActor` 标注、UIKit/ScrollView/CollectionView 桥接层的 actor 边界修正。
- 当前 `xcodebuild build -workspace .swiftpm/xcode/package.xcworkspace -scheme SectionKit-Package -destination "platform=iOS Simulator,id=DE72312F-060D-4BB9-925D-9BB79D7155D4"` 仍未通过。
- 现阶段主要阻塞点已经收敛到 `Sources/SectionKit/CollectionBaseProtocol/SKCDataSource/SKCDataSource.swift` 等 `CollectionBaseProtocol` 桥接层：
  - `UICollectionViewDataSource` / `UICollectionViewDelegate` 风格入口需要系统性补 `@MainActor`
  - 一部分主 actor 隔离后的属性访问仍在非隔离上下文中
  - 某些 `KeyPath` 用法在 actor 隔离后需要改为闭包
- 结论：仓库可以往 Swift 6.2 迁移，但不是“只改 manifest 即可”的低成本升级，建议按模块分批推进，优先清理 `CollectionBaseProtocol`，再处理测试文件里的并发捕获问题。

## Swift 6.2 升级收口

- 在同一分支继续完成 Swift 6.2 收口后，`Example` Demo 已可在模拟器成功构建、安装、启动，并能从首页进入 `Lesson 1: Standard Cell` 示例页。
- `SectionKit2.podspec` 与 `SectionUI.podspec` 的 `swift_version` 已同步更新为 `6.2`，避免 package 与 CocoaPods 声明不一致。
- `SectionKit-Package` 测试已恢复通过，结果为 `18 passed, 0 failed`。
- Swift 6.2 下测试失败的主要根因有两个：
  - `SKPublished` 测试把非 `Sendable` 发布器送入 `MainActor.run`，需要整体放到主 actor 上执行
  - `ignoreOutputType()` 返回 `any Publisher`，旧测试中对 `AnyPublisher` 的强制转换会在运行时崩溃
- 当前剩余风险主要是 warning 清理而非阻塞项，后续可以继续分模块收敛 UIKit 旧接口上的 actor 隔离告警。

## Actor Isolation Warning 清理

- 继续完成一轮 actor isolation warning 收敛后，`SectionKit-Package` 测试依旧通过，且测试输出中已不再出现 actor isolation warning。
- 本轮主要通过以下方式收敛：
  - 为 UIKit / FlowLayout / decoration / pinning 相关回调补 `@MainActor`
  - 将 `SKBindingKey.relative` 改为只在主 actor 中读取 `numberOfSections`
  - 将 `SKCSingleTypeSection.refresh` 的比较闭包收回主 actor，并避开运算符闭包导致的隔离捕获警告
- `Example` Demo 已重新构建并在模拟器前台运行，当前剩余 warning 主要是 beta API 弃用与示例代码普通 warning，不再是 Swift 6 actor isolation 阻塞项。

## Example 切到 Swift 6

- `Example.xcodeproj` 已将 `SWIFT_VERSION` 从 `5.0` 更新为 `6.0`
- 本机 `xcodebuild -showBuildSettings` 已确认 `EFFECTIVE_SWIFT_VERSION = 6`
- 在该设置下，`Example` 仍可成功构建并在模拟器前台运行，说明 Demo 已不是“Swift 5 工程 + Swift 6 包”的混合状态，而是工程侧也切到了 Swift 6 模式
