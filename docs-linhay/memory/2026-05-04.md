# 2026-05-04

## SectionUI.skills 生产模式泛化

- 更新 `SectionUI.skills` 时应沉淀通用框架模式，不应把下游使用项目做成索引。
- 可沉淀的内容包括组合式 section、fluent API、选择状态、响应式状态、嵌套横滑、装饰、曝光事件、集成层抽象边界。
- 不应沉淀下游项目路径、业务模块清单、源码文件索引或只对单个产品成立的流程。
- 追加 `SectionUI.skills/references/production-tips.md`，把下游反复出现的使用方式提炼为 61 条通用 tips，覆盖 safe size、supplementary、decoration、events/exposure、selection、reactive state、incremental updates、nested sections、styling、framework API 边界。
- 继续深挖低频高级 API，并新增 `SectionUI.skills/references/advanced-production-tips.md`：布局属性修正、collection/section plugin mode 分层、scroll observer、SKCDisplayTracker、pin 生命周期、SKWrapperView/SKCAnyViewCell、nested/page、prefetch、context menu、reorder。该文件仍保持通用框架 skill 边界，不做下游项目索引。
- 继续深挖生命周期与状态层，并新增 `SectionUI.skills/references/production-lifecycle-state.md`：SKCSectionCollector、manager 绑定与 section identity、loaded lifecycle、models/cell/supplementary publishers、reloadKind、empty supplementary、SKHighPerformanceStore、selection sequence、scroll request、SKWaterfallLayout/SKCHostingSection/SKCDragSelector 边界。仍坚持通用 skill，不沉淀下游项目索引。
- 继续深挖自定义 section 与 heterogeneous row 模式，并新增 `SectionUI.skills/references/custom-section-patterns.md`：何时直接实现 SKCSectionProtocol、row enum、注册/尺寸/配置契约、事件、增量更新、supplementary、snapshot builder、框架 API 边界。仍不记录下游项目名、路径、业务页面或扫描索引。
