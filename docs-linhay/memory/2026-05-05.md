# 2026-05-05

## SectionUI skills 深挖

- 继续将下游重复出现的 SectionUI 使用技巧泛化为通用框架 skill，明确下游仓库只作为取样来源，不在 skill 或项目文档中建立项目索引。
- 新增布局插件、Supplementary size/inset、Decoration frame、z-index 与排障 recipes，落位到 `SectionUI.skills/references/layout-decoration-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载布局与 Decoration recipes。
- 验证边界：新增内容不得包含下游项目路径、项目名、业务模块名、源码清单、扫描命中数或页面索引。

## SectionUI 交互与状态 recipes

- 继续深挖事件、曝光、选择、增量刷新、prefetch、context menu 与 reorder 的通用使用方式，新增 `SectionUI.skills/references/interaction-state-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载交互与状态 recipes。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、生命周期、选型和排障路径，不沉淀下游项目索引、页面结构或业务事件名。

## SectionUI 渲染尺寸与性能 recipes

- 继续深挖 safe-size、cellSafeSize、supplementarySafeSize、high-performance cache、fixed-size fast path、wrapper view、SKCAnyViewCell、SKCHostingSection、SKCSectionViewCell 与 SKWaterfallLayout 的通用使用方式，新增 `SectionUI.skills/references/rendering-performance-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载渲染尺寸与性能 recipes。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、尺寸/缓存/嵌套生命周期与排障路径，不沉淀下游项目布局索引、品牌 spacing 或业务 card 比例。

## SectionUI 组合渲染与样式 recipes

- 继续深挖 section 组装、SKCSectionCollector、manager reload/insert/remove、空/加载/错误状态、supplementary、section style 与 cell style 的通用使用方式，新增 `SectionUI.skills/references/composition-styling-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载组合渲染与样式 recipes。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、render builder 顺序、section identity、manager 绑定语义、样式职责与排障路径，不沉淀下游页面组装索引、业务状态名、品牌 spacing 或文案。

## SectionUI 导航滚动与分页 recipes

- 继续深挖 scroll observer、delegate forwarding、SKCDisplayTracker、manager pending scroll request、pin options、SKPageManager、SKZoomableScrollView 与请求/滚动同步策略的通用使用方式，新增 `SectionUI.skills/references/navigation-scroll-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载导航滚动与分页 recipes。
- 校正旧滚动与分页 reference 中的过期示例，避免 agents 继续使用不存在的 `SKPageManager` API 或误解 `SKCDisplayTracker` / scroll observer 的当前签名。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、滚动生命周期、曝光追踪、section/row 目标解析、pin 状态、分页身份、缩放查看与排障路径，不沉淀下游页面结构、业务 tab 名、曝光事件名、固定头高度或项目索引。

## SectionUI 响应式绑定 recipes

- 继续深挖 `SKPublished`、`SKBinding`、section model subscription、section publishers、`SKBindingKey`、result builder、`SKWhen`、`SKEventGroup`、`SKUIAction` 与反馈环控制的通用使用方式，新增 `SectionUI.skills/references/reactive-binding-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载响应式绑定 recipes。
- 校正旧 `reactive.md` 中过期或不准确的 transform 链式写法、pass-through 描述和 `sink`/`bind` 语义说明。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、状态所有权、publisher 生命周期、绑定键动态解析、事件注册清理与排障路径，不沉淀下游业务状态机、请求客户端、事件命名或项目索引。

## SectionUI 视图 Cell 与容器 recipes

- 继续深挖 `SKLoadViewProtocol`、`SKLoadNibProtocol`、`SKConfigurableView`、`SKAdaptive`、wrapper cell/view、supplementary wrapper、`SKCollectionView`、`SKCollectionViewController`、`SKUIView`、`SKUIController` 与 reuse/ownership 的通用使用方式，新增 `SectionUI.skills/references/view-cell-container-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载视图 Cell 与容器 recipes。
- 校正旧 `cell.md` 中 `SKLoadViewProtocol` 的职责描述，明确它负责 identifier/nib，尺寸来自 `SKConfigurableLayoutProtocol`。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、加载/注册/出队、配置/尺寸、Auto Layout 测量、wrapper 选型、容器生命周期与 SwiftUI bridge 所有权，不沉淀下游设计系统组件名、品牌 spacing、页面层级或项目索引。

## SectionUI Forwarding 与扩展点 recipes

- 继续深挖 manager forwarding、`SKHandleResult`、dataSource/delegate/flowLayout/prefetch 转发链、section injection、raw section wrapper 与集成边界的通用使用方式，新增 `SectionUI.skills/references/forwarding-extension-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 forwarding 与扩展点 recipes。
- 校正旧 `production-lifecycle-state.md` 中读取 section 的非公开 helper 表述，改为持有 typed section 引用或从 `manager.sections` / `sectionsPublisher` 派生。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、UIKit forwarding 顺序、observer/forward 职责、section injection 生命周期、wrapper identity 与排障路径，不沉淀下游业务 callback、埋点分类、页面策略或项目索引。

## SectionUI 拖拽框选 recipes

- 继续深挖 `SKCDragSelector`、`SKCRectSelectionManager`、`SKAutoScrollManager`、`SKSelectionOverlayView` 与 `SKLog` 的通用使用方式，新增 `SectionUI.skills/references/drag-selection-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载拖拽框选 recipes。
- 校正旧 `selection-drag-selector.md` 中过期示例，改为 `sectionView`、`section.refresh(at:)` 与 `overlayView.style`。
- 明确该组 API 是 iOS 13+ deprecated beta 能力，使用时必须关注 `setup` / `reset` 生命周期、主线程约束、选择状态所有权、自动滚动边界、手势冲突、触觉反馈与排障路径。
- 继续保持泛化边界：只沉淀 SectionUI API 语义、拖拽框选生命周期、selection delegate 契约、阈值语义、overlay/auto-scroll 配置与排障路径，不沉淀下游业务列表、页面名称、业务选中规则或项目索引。

## SectionUI 诊断与工具层 recipes

- 继续深挖 `SKPrint`、`SKPerformance`、`SKHighPerformanceStore`、`SKKVCache`、`SKCountedStore`、`SKEnvironmentConfiguration`、`SKAnimationBox`、`SKWeakBox`、`SKWeakWrapped`、`SKIDBox`、`SKInout`、`SKActorBox` 与 `SKEventGroup` 的通用使用方式，新增 `SectionUI.skills/references/diagnostics-utility-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载诊断与工具层 recipes。
- 明确 DEBUG-only 诊断、size cache key、计数 store 重置、type-keyed environment、弱引用 identity、生成 id 风险、inout builder 选型与 actor box 边界。
- 继续保持泛化边界：只沉淀 SectionUI 工具 API 语义、诊断/缓存/identity 生命周期与排障路径，不沉淀生产 telemetry、业务事件、页面排障记录或项目索引。

## SectionUI Manager 事务层 recipes

- 继续深挖 `SKCManager`、`SKCSectionInjection`、`SKCSectionActionProtocol`、`SKCSingleTypeSection`、`SKCSingleTypeSection+refresh` 与 `SKCSingleTypeSectionRowContext` 的通用使用方式，新增 `SectionUI.skills/references/manager-transaction-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 manager transaction recipes。
- 明确 manager 绑定顺序、section 对象身份、manager insert/remove/delete 语义、row mutation 与 UIKit item operation 同步、reloadKind 选型、section injection action convert、pending scroll request、绑定后访问与配置开关。
- 继续保持泛化边界：只沉淀 SectionUI manager API 语义、绑定/事务/identity 生命周期与排障路径，不沉淀具体业务模块顺序、页面状态名、业务 diff 策略或项目索引。

## SectionUI Delegate 交互层 recipes

- 继续深挖 `SKCDelegate`、`SKCDelegateProtocol`、`SKCCellActionType`、`SKCCellShouldType`、`SKCSupplementaryActionType`、`SKCContextMenuContext`、`SKUIContextMenuResult`、`SKUIAction` 与 `SKCSingleTypeSection` delegate override 的通用使用方式，新增 `SectionUI.skills/references/delegate-interaction-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 delegate interaction recipes。
- 明确 delegate routing、highlight/select gates、primary action、display lifecycle、focus/editing、spring-load、UIKit multiple selection、context menu 单 item 语义、reorder move gate、section subclassing 与 integration forwarding 边界。
- 继续保持泛化边界：只沉淀 SectionUI delegate API 语义、UIKit gate 默认值、section override 选型与排障路径，不沉淀具体业务导航、菜单内容、权限规则、页面状态或项目索引。

## SectionUI SwiftUI Hosting recipes

- 继续深挖 `SKUIView`、`SKUIController`、`SKPreview`、`SKCHostingCollectionView`、`SKCHostingSection`、`STCHostingCell` 与 `SKExistModelProtocol` 的通用使用方式，新增 `SectionUI.skills/references/swiftui-hosting-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 SwiftUI hosting recipes。
- 明确 UIKit-in-SwiftUI、SwiftUI-in-SectionUI row、SwiftUI embedding whole SectionUI collection 三类 bridge 的所有权差异，以及 iOS 16+ hosting、computed section identity、`objectIdentifier` reload 判断、zero margins、preview helper 与 state ownership 边界。
- 校正旧 `layout-plugins.md` 中 waterfall ratio 示例，明确 `columnWidth(ratios:)` 的比例和必须为 `1.0`。
- 继续保持泛化边界：只沉淀 SectionUI SwiftUI bridge API 语义、hosting identity、sizing/state 生命周期与排障路径，不沉淀具体 SwiftUI 业务组件、页面名称、路由、请求客户端或项目索引。

## SectionUI Safe Size 测量 recipes

- 继续深挖 `SKSafeSizeProvider`、`cellSafeSize`、`SKCCellFractionLayoutContext`、`SKSafeSizeTransform`、`supplementarySafeSize` 与 `SKSupplementaryKind` 的通用测量语义，新增 `SectionUI.skills/references/safe-size-measurement-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 safe size measurement recipes。
- 校正旧 `performance.md` 与 `section-advanced-2.md` 中不存在的 `.aspectRatio`、`.inset`、`.subtract` transform 示例，改为当前公开 API 或显式 `SKSafeSizeTransform`。
- 明确 `cellSafeSize` 是传给 `preferredSize(limit:model:)` 的测量边界，不是最终 item size；默认 safe size 按滚动方向扣除 inset，fraction grid 会结合 `minimumInteritemSpacing` 并 floor 宽度，`supplementarySafeSize(.apple)` 使用 collection bounds。
- 继续保持泛化边界：只沉淀 SectionUI 测量链路、provider fallback、transform 顺序、supplementary provider 与缓存排障路径，不沉淀具体页面比例、业务 spacing、下游路径、项目名或扫描索引。

## SectionUI 条件渲染与 Builder recipes

- 继续深挖 `SectionArrayResultBuilder`、`SKCSectionCollector`、`SKWhen`、`SKBindingKey` 与 `SKCHostingCollectionView` builder 的通用渲染组装语义，新增 `SectionUI.skills/references/render-builder-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 render builder recipes。
- 明确 builder 适合声明式 section/model 数组，collector 适合 optional object 过滤与转换，`SKWhen` 适合复用谓词，`SKBindingKey` 适合动态 section index，SwiftUI hosted collection 通过 `objectIdentifier` 序列判断是否 reload。
- 继续保持泛化边界：只沉淀 SectionUI 条件组装、predicate、动态 index binding、identity 与排障路径，不沉淀具体页面模块顺序、业务状态名、路由、埋点或项目索引。

## SectionUI Layout Plugin 执行语义 recipes

- 继续深挖 `SKCollectionFlowLayout`、`SKCLayoutPlugins.Mode`、section-level layout plugins、`setAttributes`、`SKCPluginLayoutAttributesForElementsForward`、layout store 与 invalidation 的通用执行语义，新增 `SectionUI.skills/references/layout-plugin-execution-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 layout plugin execution recipes。
- 校正旧 `layout-plugins.md` 与 `advanced-production-tips.md` 中过期或不准确的示例：supplementary singleton mode 应通过 `sectionView.set(pluginModes:)` 使用，section 级 one-off supplementary correction 使用 `setAttributes(...)`，attribute adjustment 使用 `SKCPluginAdjustAttributes` / `SKInout`。
- 明确 layout mode 按 priority 执行而非添加顺序；`.attributes`、alignment、decoration、full-attribute forward 会合并，supplementary size/inset/adjustment 是 singleton-by-priority；scroll-dependent forward 需要谨慎设置 `alwaysInvalidate` 并管理 cancellation。
- 继续保持泛化边界：只沉淀 SectionUI layout plugin 执行顺序、scope、invalidation、attribute mutation 与排障路径，不沉淀视觉 preset、业务模块顺序、品牌样式、下游路径或扫描索引。

## SectionUI Selection 状态所有权 recipes

- 继续深挖 `SKSelectionState`、`SKSelectionProtocol`、`SKSelectionWrapper`、`SKSelectionSequence` 与 `SKSelectionIdentifiableSequence` 的通用选择状态语义，新增 `SectionUI.skills/references/selection-ownership-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 selection ownership recipes。
- 校正旧 `selection.md`、`selection-protocol.md`、`selection-sequence.md`、`selection-identifiable-sequence.md` 中过期或不准确的示例：不再对只读 `isSelected` 赋值，不再使用 wrapper `.value`，不再手动用引用比较实现单选，明确 `append` 不重建 per-item observation。
- 明确 selection state 是引用对象，wrapper equality/hash 使用 `id`，`SKSelectionSequence` 适合 row-offset identity，`SKSelectionIdentifiableSequence` 适合过滤/排序/重排，`isUnique == true` 下 `selectAll()` 不会形成真正多选。
- 继续保持泛化边界：只沉淀 SectionUI selection state ownership、identity、publisher 生命周期、cell reuse binding 与排障路径，不沉淀业务权限、页面规则、状态名、下游路径或扫描索引。

## SectionUI Prefetch / Menu / Reorder recipes

- 继续深挖 `SKCPrefetch`、`loadMorePublisher`、`onContextMenu`、`SKUIContextMenuResult`、`SKUIAction`、`onCellShould(.move)` 与 `SKCSingleTypeSection.move(from:to:)` 的通用交互扩展语义，新增 `SectionUI.skills/references/prefetch-menu-reorder-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 prefetch menu reorder recipes。
- 校正旧 `section-advanced.md`、`reactive.md`、`performance.md` 中过期或不准确的示例：`.canMove` 改为 `.move`，section prefetch publisher 明确发 section-local rows，不再写成 `IndexPath`，cross-section move 不再描述为自动完整处理。
- 明确 `loadMorePublisher` 由最大 prefetched row 触达当前末尾触发，需要 request gate；`SKCContextMenuContext` 不支持取 cell view；`SKUIAction` 的 async throwing handler 需要内部处理错误；同 section reorder 默认 swap，跨 section 需要显式处理。
- 继续保持泛化边界：只沉淀 SectionUI prefetch row 语义、pagination gate、context menu routing、async action ownership、reorder 默认行为与排障路径，不沉淀网络客户端、业务菜单、权限规则、页面名、下游路径或扫描索引。

## SectionUI Cache / Exposure recipes

- 继续深挖 `SKHighPerformanceStore`、`SKKVCache`、`SKCountedStore`、`displayedTimes` 与 `model(displayedAt:)` 的通用缓存和曝光计数语义，新增 `SectionUI.skills/references/cache-exposure-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 cache exposure recipes。
- 校正旧 `performance.md`、`section-advanced-2.md`、`MISSING_FEATURES.md`、`layout-plugins.md`、`advanced-sections.md` 与 `production-tips.md` 中过期或不准确的示例：不再使用 `highPerformanceStore?.removeValue`、`highPerformance?.clear()`、`displayedAt: .at(...)`、`context.displayedTimes`、`context.displayCount`、`displayedTimes.reset(row:)` 或无 context 的 `safeSizeProvider.size`。
- 明确 `SKHighPerformanceStore` 按 `(id, limitSize)` 缓存，外部需要手动失效时应持有 store；`SKKVCache` 基于 `NSCache` 且 count 不是持久指标；`displayedTimes` 对 single-type section 是 row-based，需要在模型宇宙替换或嵌套 section 复用时重置。
- 继续保持泛化边界：只沉淀 SectionUI cache identity、limit-size key、cache invalidation ownership、row-based exposure reset 与排障路径，不沉淀埋点事件、业务 ID、页面规则、下游路径或扫描索引。

## SectionUI Adaptive Sizing recipes

- 继续深挖 `SKAdaptive`、`SKConfigurableAdaptiveView`、`SKConfigurableAdaptiveMainView`、`SKConfigurableAutoAdaptiveView` 与 Auto Layout fitting 的通用测量语义，新增 `SectionUI.skills/references/adaptive-sizing-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 adaptive sizing recipes。
- 校正 skill 示例中的不完整单参数 `SKAdaptive` 泛型示例，改为 `SKAdaptive<View, Model>` 或 `SpecializedAdaptive`；校正 adaptive cell 示例仍需 `SKLoadViewProtocol` 的约束。
- 明确 vertical adaptive 是固定宽度测高度、horizontal adaptive 是固定高度测宽度；`content` key path 只在 fittingSizeLevel 轴覆盖测量结果；`SKConfigurableAdaptiveAutoCache` 缓存 adapter 实例，不缓存每个模型的 size。
- 继续保持泛化边界：只沉淀 SectionUI adaptive measurement pipeline、fitting priority、content override、insets、auto cache 与 size-cache invalidation，不沉淀具体页面排版、品牌 spacing、业务展开规则、下游路径或扫描索引。

## SectionUI Index Title recipes

- 继续深挖 `indexTitle`、`indexTitleRow`、`sectionIndex`、iOS 14+ collection index titles 与 data-source forwarding 的通用导航元数据语义，新增 `SectionUI.skills/references/index-title-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 index title recipes。
- 校正旧 `MISSING_FEATURES.md` 与 `section-advanced-2.md` 中不存在的 manager update 示例，改为 `manager.reload(sectionsWithIndex)`。
- 明确 SectionUI 从最终 bound sections 收集非空 `indexTitle`，按 filtered title position 找到目标 section，再返回当前 `sectionInjection.index` 与 `indexTitleRow` 组成的 index path；`indexTitleRow` 默认是 0。
- 继续保持泛化边界：只沉淀 SectionUI index title contract、lookup semantics、forwarding boundary、reload timing 与排障路径，不沉淀具体字母表、业务分类、页面顺序、下游路径或扫描索引。

## SectionUI Row Mutation recipes

- 继续深挖 `SKCSingleTypeSection+refresh`、row mutation、`RefreshPayload`、predicate refresh、append/insert/remove/delete、`apply` 与 `reloadKind` 的通用数据操作语义，新增 `SectionUI.skills/references/row-mutation-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 row mutation recipes。
- 校正旧 `performance.md` 中不存在的“向 section reload 传入 ReloadKind”示例，改为设置 `reloadKind` 后调用 `apply(newModels)`；校正 `section-best-practices.md` 中 bound row mutation 后无条件追加 `manager.reload()` 的误导。
- 明确 `refresh(at:)` 只触发 item reload、不写模型；`refresh(with:)` 会先写模型再 reload rows；predicate refresh 只替换已匹配行，不负责插入或删除。
- 继续保持泛化边界：只沉淀 SectionUI row mutation API 顺序、source-of-truth、identity predicate、reloadKind 与 async row 重解析，不沉淀业务回滚策略、请求客户端、埋点、页面流程、下游路径或扫描索引。

## SectionUI Supplementary recipes

- 继续深挖 header/footer、`setHeader`、`setFooter`、`set(supplementary:type:model:)`、dynamic supplementary model、隐藏规则、移除语义、lifecycle action 与 custom kind 边界，新增 `SectionUI.skills/references/supplementary-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 supplementary recipes。
- 校正旧 `section-styling.md` 中 supplementary/header/footer `preferredSize` 示例签名；校正按 view type 移除普通 `.header` 的误导，改为按 `.header` / `.footer` kind 移除。
- 明确 supplementary set 会 queue registration 并 reload；dynamic model closure 返回 nil 时 size 为 `.zero` 且跳过 config；`hiddenHeaderWhenNoItem` / `hiddenFooterWhenNoItem` 默认 true；single-type section 默认只返回 header/footer，不返回 custom kind。
- 继续保持泛化边界：只沉淀 SectionUI supplementary registration、sizing、visibility、removal、lifecycle 与 custom-kind API 语义，不沉淀具体 header 文案、业务分组、设计系统组件、埋点、页面结构、下游路径或扫描索引。

## SectionUI Container Lifecycle recipes

- 继续深挖 `SKCollectionView`、`SKCollectionViewController`、`reloadSections`、`controllerStyle`、`sectionViewStyle`、`ignoresSafeArea`、`refreshable`、layout invalidation、`scrollDirection` 与 collection-level plugin modes 的通用容器生命周期语义，新增 `SectionUI.skills/references/container-lifecycle-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 container lifecycle recipes。
- 更新 `view-cell-container-recipes.md`，把深入容器生命周期细节导向新 reference。
- 明确 `reloadSections` / `controllerStyle` / `sectionViewStyle` 在 view load 前会排队到 `viewDidLoad.after`；`ignoresSafeArea()` 只切换 top 约束；`refreshable` action 返回后才 `endRefreshing()`；`SKCollectionView` 的 collection-level plugin modes 会在 flow layout fetch 阶段与 section-level plugins 合并。
- 继续保持泛化边界：只沉淀 SectionUI container lifecycle、manager ownership、safe-area、refreshable、layout invalidation、plugin mode 与 pending request 语义，不沉淀具体页面加载策略、路由、请求客户端、埋点、产品状态流、下游路径或扫描索引。

## SectionUI Nested Section Cell recipes

- 继续深挖 `SKCSectionViewCell`、`SKCSingleSectionViewCell`、`wrapperToHorizontalSection` 与嵌套 collection row 的通用使用方式，新增 `SectionUI.skills/references/nested-section-cell-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 nested section cell recipes。
- 校正旧 `production-usage.md` 与 `advanced-sections.md` 中 deprecated 或不存在的嵌套 section 示例：不再推荐旧的 ViewCell 后缀包装方法，不再使用不存在的子类 section-setup override。
- 明确 child sections 通过 `SKCSectionViewCell.Model` 传入，`config(_:)` 会设置内部 `sectionView`、应用 style/insets 并 reload child sections；内部 manager 设置 `supportUnbindSection = false`，嵌套曝光、选择、prefetch、scroll observer 与订阅清理必须在父模型 rebinding 时显式处理。
- 继续保持泛化边界：只沉淀 SectionUI nested section cell API 语义、尺寸、内部 collection 生命周期、状态重置与排障路径，不沉淀下游项目路径、仓库名、业务模块、页面结构、业务事件或源码索引。

## SectionUI Raw Section Wrapper recipes

- 继续深挖 `SKCRawSectionProtocol`、`SKCAnySectionProtocol`、`SKCAnySingleTypeSectionProtocol` 与 reusable raw-section wrapper 的通用使用方式，新增 `SectionUI.skills/references/raw-section-wrapper-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 导航，使 agents 能按需加载 raw section wrapper recipes。
- 更新 `forwarding-extension-recipes.md` 与 `custom-section-patterns.md`，把 section wrapper 协议、wrapper identity 与 direct custom section 选型边界导向新 reference。
- 明确 wrapper 的 `section` / `rawSection` 必须稳定并指向最终绑定 manager 的同一 section 实例；`SKCManager` 仍绑定 `SKCBaseSectionProtocol`，wrapper 需通过 `.section` 或 `SKCSectionCollector` 解包；`setSectionStyle`、`plugins`、`pin(options:)` 与 `onCellAction(...)` 都是转发到 raw section 的生命周期行为。
- 继续保持泛化边界：只沉淀 SectionUI wrapper protocol、identity、style/plugin forwarding、event forwarding 与生命周期语义，不沉淀下游项目路径、仓库名、业务模块、页面结构、业务事件或源码索引。

## SectionUI Runtime View Wrapper recipes

- 继续深挖 `SKCAnyViewCell`、`SKWrapperView`、`SKCWrapperCell` 与 `SKCWrapperReusableView` 的通用 runtime view wrapper 使用方式，新增 `SectionUI.skills/references/runtime-view-wrapper-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 与 `view-cell-container-recipes.md` 导航，使 agents 能按需加载 runtime view wrapper recipes。
- 明确 `SKCAnyViewCell` 承载 live `UIView` 实例并会移动 superview ownership；`SKWrapperView` 的 insets 同时影响测量和约束；`SKCWrapperCell` 支持 nib-backed wrapped view，而 `SKCWrapperReusableView` 当前直接创建 `View()`。
- 继续保持泛化边界：只沉淀 SectionUI runtime view hosting、wrapper sizing、nib 行为、reuse ownership 与排障路径，不沉淀下游设计系统组件、页面结构、业务事件或源码索引。

## SectionUI Page And Zoom recipes

- 继续深挖 `SKPageManager`、`SKPageViewController`、`SKPageChildController`、`SKZoomableScrollView` 与 `SKZoomableContext` 的通用分页/缩放使用方式，新增 `SectionUI.skills/references/page-zoom-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 与 `navigation-scroll-recipes.md` 导航，使 agents 能按需加载 page and zoom recipes。
- 明确 page child controller cache 以 child `id` 为 key；`current` 可在 UI 未绑定时更新但 controller 可能为 nil；`SKPageViewController` 对 `scrollDirection`、`spacing`、`childs` 变化 debounce 后重建；zoom layout 由 `context.size` 驱动并会 reset zoom。
- 校正 `navigation-scroll-recipes.md` 中 double tap gesture 语义：double tap 始终可用于自定义 action 或默认 zoom toggle，single tap / long press 才按 action 是否存在启用。
- 校正 `page.md` 中直接写 `contentView` / `zoomScale` / `zoom(to:)` 的旧示例，改为当前源码支持的 `SKZoomableContentView` + `wrapperToZoomableView()` 用法。
- 继续保持泛化边界：只沉淀 SectionUI page identity/cache、selection/current binding、controller lifecycle、zoom sizing、tap actions 与 pan-to-dismiss 机制，不沉淀下游页面名、路由、图片加载器、业务事件或源码索引。

## SectionUI Section Assembly Identity recipes

- 继续深挖 `SectionArrayResultBuilder`、`SKCSectionCollector`、`SKWhen`、`SKBindingKey` 与 `SKCHostingCollectionView` 的通用动态 section 组装和 identity 规则，新增 `SectionUI.skills/references/section-assembly-identity-recipes.md`。
- 更新 `SectionUI.skills/SKILL.md` 与 `render-builder-recipes.md` 导航，使 agents 能按需加载 section assembly identity recipes。
- 明确 builder 是纯 flatten；collector 存储 `[SKCSectionProtocol]` 并会解包 `SKCAnySectionProtocol.section`；`SKBindingKey` 的 equality/hash 基于当前 wrapped value；`SKCHostingCollectionView` 只在 `objectIdentifier` 序列变化时 reload。
- 继续保持泛化边界：只沉淀 SectionUI builder flattening、collector filtering/conversion、predicate composition、dynamic section-index binding 与 identity 规则，不沉淀下游模块顺序、业务状态名、页面结构、路由、埋点或源码索引。
