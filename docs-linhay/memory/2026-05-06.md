# 2026-05-06

## SectionUI 数据驱动最佳实践

- 明确 SectionUI 框架宗旨：绑定完成后，业务只管理数据和状态，UI 变化由 SectionUI 从模型、选择状态、publisher 与 section mutation 自然派生。
- 新增 `SectionUI.skills/references/data-driven-best-practices.md`，沉淀绑定期/运行期职责、source of truth、推荐更新流、组合规则、反模式和排障清单。
- 更新 `SectionUI.skills/SKILL.md` 与 `README.md`，让 agents 和用户都能优先看到“绑定后只管理数据”的核心规则。
- 继续保持泛化边界：只沉淀 SectionUI 框架级 API 语义、状态所有权和排障路径，不沉淀下游项目路径、项目名、业务模块名、页面索引、扫描来源或统计数字。

## SectionUI.skills 查找路径整理

- 对 `SectionUI.skills/SKILL.md` 做入口信息架构整理，将连续的 reference 描述改为 `Reference Lookup Workflow`、`Task Router` 与 `API Keyword Map`。
- 更新 frontmatter description，增加数据源所有权、row mutation、section builders、SwiftUI hosting 等检索关键词，提升 skill 触发与定位效果。
- 保留完整 `Reference Documentation`，确保所有 reference 仍然从入口一层可达。
- 继续保持泛化边界：入口只说明框架级查找路径、API 关键词和任务路由，不沉淀下游项目索引、页面结构、业务模块或扫描统计。

## SectionUI.skills reference 触发表述规范化

- 将多篇主要 reference 的首段从 “This reference captures...” 改为 “Use this reference when...”，提升 agent 触发判断、qmd 检索和人工扫读效率。
- 覆盖 manager transaction、reactive binding、composition/styling、interaction state、layout/decoration、rendering/performance、SwiftUI hosting、nested section、raw wrapper、forwarding/extension、diagnostics utility 等方向。
- 补强首段关键词：source-of-truth、binding lifecycle、section identity、row operation、cache invalidation、feedback loop、nested collection lifecycle、hosting sizing、delegate gates、layout invalidation。
- 继续保持泛化边界：只沉淀 SectionUI 框架级适用场景和 API 关键词，不写下游项目路径、项目名、业务模块、页面索引或扫描统计。

## SectionUI.skills UI 元数据补充

- 新增 `SectionUI.skills/agents/openai.yaml`，提供 UI 展示名、短描述、默认调用 prompt 与隐式触发策略。
- 未添加图标、外部工具依赖或额外说明文档，保持 skill 元数据最小化。
